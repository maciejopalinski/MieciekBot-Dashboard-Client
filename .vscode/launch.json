{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Chrome",
            "request": "attach",
            "type": "pwa-chrome",
            "urlFilter": "http://*:8080/*",
            "webRoot": "${workspaceFolder}/dashboard-client",
            "port": 9222
        }
    ]
}